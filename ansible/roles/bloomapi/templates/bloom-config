var config = {
  sql: {
    host: 'localhost',
    port: '5432',
    username: 'postgres',
    password: '{{ postgres_password }}',
    dbname: 'bloomapi'
  },
  googleAnalytics: {
    trackingID: '{{ ga_tracking_id }}', // Optional, leave blank to disable GA
    domain: '{{ ga_domain }}' // Optional, leave blank to disable GA
  }
};

module.exports = config;
