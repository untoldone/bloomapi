sqlConnStr = "postgres://postgres@db/bloomapi?sslmode=disable"
searchHosts = [
  "search"
]
bloomapiPort = 3005
yourchartUrl = "http://yourchart:3001"
generalLogger = "stdout"
#statsLogger = "syslog" # values can be: 'syslog' or 'stdout', default is nothing
