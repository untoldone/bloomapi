var config = {
  sql: {
    host: 'localhost',
    port: '5432',
    username: 'postgres',
    password: 'postgres',
    dbname: 'bloomapi'
  },
  googleAnalytics: {
    trackingID: 'UA-XXXXXXXX-1', // Optional, leave blank to disable GA
    domain: 'yourdomain.com' // Optional, leave blank to disable GA
  }
};

module.exports = config;
